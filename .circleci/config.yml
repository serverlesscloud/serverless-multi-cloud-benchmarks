version: 2
jobs:
  build:
    docker:
      - image: flemay/musketeers

    steps:
      - checkout
      - setup_remote_docker
      - run:
          name: package
          command: make package
          environment:
            PROVIDERS: aws